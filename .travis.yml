language: csharp
sudo: required
addons:
  apt:
    packages:
    - dotnet-sharedframework-microsoft.netcore.app-1.1.2

env: CONFIGS=Release
os: linux
dotnet: 2.1.4

matrix:
  include:
    - mono: latest
  exclude:
    - env: CONFIGS=Release

script: ./run tests
