image: Visual Studio 2019

artifacts:
  - path: /Libs/Hopac/bin/Release/*.nupkg

build_script:
  - cmd: dotnet build Hopac.sln -c Release

deploy:
  - provider: NuGet
    api_key:
      secure: MY_SECRET_KEY_HERE
    on:
      appveyor_repo_tag: true
